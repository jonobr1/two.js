{
  "name": "two-js-types-tests",
  "version": "1.0.0",
  "description": "Tests for the Two.js TypeScript type definitions",
  "scripts": {
    "test": "jest --env=jsdom",
    "test:watch": "jest --env=jsdom --watch",
    "test:coverage": "jest --env=jsdom --coverage"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "jest-environment-jsdom": "^29.7.0",
    "ts-jest": "^29.1.0",
    "two.js": "file:../..",
    "typescript": "^5.0.0"
  }
}
